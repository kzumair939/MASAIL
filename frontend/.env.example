# Base URL the React app calls for the Spring Boot API.
# Local dev (vite dev server + backend on 8080 with proxy below): /api
# Standalone dev without proxy: http://localhost:8080/api
VITE_API_BASE_URL=/api
